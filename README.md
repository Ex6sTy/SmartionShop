# Магазин
В проекте реализованы классы `Product` и `Category`.
- Класс `Product` описывает товары с их характеристиками.
- Класс `Category` управляет категориями и автоматически подсчитывает их количество.
